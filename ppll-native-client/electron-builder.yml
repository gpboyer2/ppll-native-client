appId: com.ppll.native-client
productName: PPLL量化交易终端
directories:
  output: dist
  buildResources: build
files:
  - out/**/*
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
  - src/main/backend/node_modules/{sqlite3,better-sqlite3}/**
extraResources:
  - from: src/main/backend
    to: backend
    filter:
      - package.json
      - .sequelizerc
      - '**/*.js'
      - config/**
      - models/**
      - migrations/**
copyright: Copyright 2025 PPLL Team
win:
  executableName: ppll-native-client
  icon: resources/icon.ico
  legalTrademarks: Copyright 2025 PPLL Team
  requestedExecutionLevel: asInvoker
nsis:
  artifactName: ${name}-${version}-${arch}-setup.${ext}
  shortcutName: PPLL量化交易终端
  uninstallDisplayName: PPLL量化交易终端
  createDesktopShortcut: always
  oneClick: false
  allowToChangeInstallationDirectory: true
  perMachine: false
  runAfterFinish: false
  installerIcon: resources/icon.ico
  uninstallerIcon: resources/icon.ico
mac:
  identity: null
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
  hardenedRuntime: false
  icon: resources/icon.icns
  category: public.app-category.finance
dmg:
  artifactName: ${name}-${version}-${arch}.${ext}
linux:
  target:
    - AppImage
    - deb
  executableName: ppll-native-client
  maintainer: ppll
  category: Finance
  icon: resources/icon.png
deb:
  artifactName: ${name}-${version}-${arch}.${ext}
appImage:
  artifactName: ${name}-${version}-${arch}.${ext}
npmRebuild: true
publish:
  provider: github
  owner: gpboyer2
  repo: ppll-native-client
