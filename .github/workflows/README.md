# GitHub Actions 快速开始

## 5 分钟上手指南

### 第一步：推送代码到 GitHub

```bash
# 如果还没有推送到 GitHub
git remote add origin https://github.com/你的用户名/你的仓库.git
git push -u origin master
```

### 第二步：触发构建

**方式 A：推送代码自动触发**
```bash
# 修改代码后推送
git add .
git commit -m "feat: 新功能"
git push origin master
```

**方式 B：手动触发**
1. 打开 GitHub 仓库页面
2. 点击 `Actions` 标签
3. 选择 `Build Multi-Platform`
4. 点击 `Run workflow` → `Run workflow`

**方式 C：创建 Release（推荐）**
```bash
# 创建版本标签并推送
git tag v1.0.0
git push origin v1.0.0
```

### 第三步：查看构建进度

1. 在 GitHub 仓库页面点击 `Actions`
2. 看到黄色的圆点表示构建中 🟡
3. 看到绿色的勾表示构建成功 ✅
4. 看到红色的叉表示构建失败 ❌

### 第四步：下载构建产物

**从 Actions 下载：**
1. 点击成功的构建记录
2. 滚动到页面底部的 `Artifacts` 区域
3. 下载你需要的平台包：
   - `ppll-client-mac-intel-x64` - Intel Mac
   - `ppll-client-mac-apple-arm64` - Apple Silicon Mac
   - `ppll-client-mac-universal` - 通用版 Mac
   - `ppll-client-win-x64` - Windows 64位
   - `ppll-client-win-arm64` - Windows ARM64

**从 Release 下载（仅标签构建）：**
1. 点击仓库的 `Code` 标签
2. 点击右侧 `Releases`
3. 找到对应的版本（如 v1.0.0）
4. 下载需要的安装包

## 常用命令

```bash
# 创建发布版本
git tag v1.0.0
git push origin v1.0.0

# 删除本地标签
git tag -d v1.0.0

# 删除远程标签
git push origin --delete refs/tags/v1.0.0

# 查看所有标签
git tag -l

# 跳过 CI 构建
git commit -m "docs: 更新文档 [skip ci]"
git push
```

## 工作流说明

### 自动触发条件

| 触发方式 | 说明 | 是否创建 Release |
|---------|------|-----------------|
| 推送到 master/develop | 每次 push 都触发 | ❌ 否 |
| 推送标签 (v*) | 推送版本标签时触发 | ✅ 是 |
| Pull Request | 创建/更新 PR 时触发 | ❌ 否 |
| 手动触发 | 在网页上手动运行 | ❌ 否 |

### 构建平台

```
macOS (3个)
├── Intel x64     → ppll-client-v1.0.0-mac-intel-x64.dmg
├── Apple ARM     → ppll-client-v1.0.0-mac-apple-arm64.dmg
└── Universal     → ppll-client-v1.0.0-mac-universal.dmg

Windows (2个)
├── x64           → ppll-client-v1.0.0-win-x64.exe
└── ARM64         → ppll-client-v1.0.0-win-arm64.exe
```

### 构建时间

- macOS 单平台：5-10 分钟
- Windows 单平台：5-10 分钟
- 总计（5个并行）：10-15 分钟

## 故障排查

### 构建失败

1. 点击红色的 ❌ 查看详细日志
2. 找到具体的错误信息
3. 检查是否是依赖问题或代码错误
4. 修复后重新推送

### 常见错误

**错误：依赖安装失败**
```
解决：检查 wails.json 中的依赖版本是否正确
```

**错误：构建超时**
```
解决：GitHub Actions 有时间限制，检查是否有死循环
```

**错误：Windows 构建失败**
```
解决：Windows 路径问题，检查文件路径是否使用了 /
```

## 下一步

- 📖 阅读详细文档：[GITHUB_ACTIONS_GUIDE.md](../GITHUB_ACTIONS_GUIDE.md)
- 🔧 自定义工作流：编辑 `.github/workflows/build.yml`
- 🚀 优化构建速度：添加缓存、减少平台数量

## 需要帮助？

- 查看 [GitHub Actions 文档](https://docs.github.com/en/actions)
- 查看 [Wails 文档](https://wails.io/docs/introduction)
- 提交 Issue 到项目仓库
